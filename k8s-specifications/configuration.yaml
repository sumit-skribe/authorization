apiVersion: v1
kind: Pod
metadata:
  name: authorization
  namespace: searchcombat
  labels:
    env: production
spec:
  containers:
  - name: authorization
    image: sumit0skribe/authorization:sha-bbb0360
    imagePullPolicy: Always
    ports:
    - containerPort: 3000
    resources:
      requests:
        memory: "64Mi"
        cpu: "250m"
      limits:
        memory: "128Mi"
        cpu: "500m"